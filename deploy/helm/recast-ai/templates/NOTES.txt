Recast AI {{ .Chart.AppVersion }} has been installed as release {{ .Release.Name }}.

Pods:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}

API Gateway service:
  kubectl get svc {{ .Release.Name }}-api-gateway

Tail logs from a service:
  kubectl logs -l app.kubernetes.io/component=api-gateway -f

{{- if .Values.ingress.enabled }}

Ingress:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}
{{- else }}

To expose the gateway locally:
  kubectl port-forward svc/{{ .Release.Name }}-api-gateway 8080:8080
{{- end }}

Required secret keys (set via secrets.data or an external secret manager):
  - JWT_SECRET
  - GEMINI_API_KEY
  - ELEVENLABS_API_KEY (optional)
  - AWS_ACCESS_KEY_ID  (optional, for Polly + S3)
  - AWS_SECRET_ACCESS_KEY (optional)
